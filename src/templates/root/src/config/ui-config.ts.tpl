export interface {{UI_CONFIG_INTERFACE}} {
  enabled: boolean;
}

export const defaultPluginConfig: {{UI_CONFIG_INTERFACE}} = {
  enabled: true,
};

export const {{UI_CONFIG_KEY}} = '{{UI_CONFIG_KEY}}';
